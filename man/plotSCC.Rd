% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSCC.R
\name{plotSCC}
\alias{plotSCC}
\title{Visualize SCC Results}
\usage{
plotSCC(
  SCC_Result,
  title = "SCC Visualization",
  colorPalette = "viridis",
  showPoints = TRUE,
  alphaLevel = 0.05,
  xlab = NULL,
  ylab = NULL
)
}
\arguments{
\item{SCC_Result}{A list containing SCC computation results from \code{ImageSCC::scc.image()}.}

\item{title}{A character string specifying the title of the plot. Default is `"SCC Visualization"`.}

\item{colorPalette}{A character string specifying the color scheme. Options: `"viridis"`, `"magma"`, `"turbo"`. Default is `"viridis"`.}

\item{showPoints}{A logical indicating whether to overlay significant SCC points. Default is \code{TRUE}.}

\item{alphaLevel}{A numeric value specifying the confidence level threshold for SCC detection. Default is \code{0.05}.}

\item{xlab}{A character string specifying the x-axis label. Defaults to `"Longitudinal (1-X)"`, where `X` is extracted from SCC dimensions.}

\item{ylab}{A character string specifying the y-axis label. Defaults to `"Transversal (1-Y)"`, where `Y` is extracted from SCC dimensions.}
}
\value{
A heatmap plot displaying SCC values with optional overlays of significant points.
}
\description{
This function visualizes Simultaneous Confidence Corridor (SCC) results as a heatmap,
with the option to overlay significant points where detected differences exceed the confidence bands.
}
\details{
- Extracts SCC matrix values and formats them for visualization.
- Uses \code{getPoints()} to identify significant regions exceeding SCC thresholds.
- Differentiates **positive** and **negative** SCC points using distinct markers.
}
\examples{
\dontrun{
  # Load SCC results
  load("SCC_COMP.RData")

  # Basic SCC visualization
  plotSCC(SCC_COMP)

  # SCC plot with specific confidence threshold and custom color palette
  plotSCC(SCC_COMP, alphaLevel = 0.01, colorPalette = "magma", title = "SCC Analysis (alpha = 0.01)")
}

}
